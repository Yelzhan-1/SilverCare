# GEMINI.md

@AGENTS.md
@DESIGN.md

The design system is enforced by the `super-design` skill. Before touching UI, read `DESIGN.md` and follow the token-first rules in `AGENTS.md`.

Framework adapter references live in `.claude/skills/super-design/references/framework-adapters/`.
